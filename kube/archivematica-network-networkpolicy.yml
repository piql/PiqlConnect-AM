apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: archivematica-network
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/archivematica-network: "true"
  podSelector:
    matchLabels:
      io.kompose.network/archivematica-network: "true"
